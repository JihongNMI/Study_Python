# 1. 파이선에서 requests랑 urllib.request는 다른건가?
- urllib.request : 	표준 라이브러리 (파이썬에 기본 포함)
- requests : 서드파티 라이브러리 (외부 설치 필요: pip install requests)

# 2-1. 책 오타 발견

어디서는(수업)

import urllib.request

req = urllib.request.Request(url)

라고 하고



어디서는(책)

from urllib.request import urlopen, Request

req = request(url)

이라고 하던데 뭐가 달라?

# 2-2. from 쓰는 법

